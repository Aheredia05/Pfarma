{% extends 'base.html.twig' %}

{% block title %}Detallefactura{% endblock %}

{% block body %}
    <h1>Detallefactura</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ detallefactura.id }}</td>
            </tr>
            <tr>
                <th>Cantidad</th>
                <td>{{ detallefactura.cantidad }}</td>
            </tr>
            <tr>
                <th>Preciototal</th>
                <td>{{ detallefactura.preciototal }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('detallefactura_index') }}">back to list</a>

    <a href="{{ path('detallefactura_edit', {'id': detallefactura.id}) }}">edit</a>

    {{ include('detallefactura/_delete_form.html.twig') }}
{% endblock %}
