{% extends 'base.html.twig' %}

{% block title %}Proveedor index{% endblock %}

{% block body %}
    <h1>Proveedor index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Proveedor</th>
                <th>Contacto</th>
                <th>Telefono</th>
                <th>Direccion</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for proveedor in proveedors %}
            <tr>
                <td>{{ proveedor.id }}</td>
                <td>{{ proveedor.proveedor }}</td>
                <td>{{ proveedor.contacto }}</td>
                <td>{{ proveedor.telefono }}</td>
                <td>{{ proveedor.direccion }}</td>
                <td>
                    <a href="{{ path('proveedor_show', {'id': proveedor.id}) }}">show</a>
                    <a href="{{ path('proveedor_edit', {'id': proveedor.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="6">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('proveedor_new') }}">Create new</a>
{% endblock %}
