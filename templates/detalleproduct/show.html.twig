{% extends 'base.html.twig' %}

{% block title %}Detalleproduct{% endblock %}

{% block body %}
    <h1>Detalleproduct</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ detalleproduct.id }}</td>
            </tr>
            <tr>
                <th>Cantidad</th>
                <td>{{ detalleproduct.cantidad }}</td>
            </tr>
            <tr>
                <th>Precioventa</th>
                <td>{{ detalleproduct.precioventa }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('detalleproduct_index') }}">back to list</a>

    <a href="{{ path('detalleproduct_edit', {'id': detalleproduct.id}) }}">edit</a>

    {{ include('detalleproduct/_delete_form.html.twig') }}
{% endblock %}
